name: UI Performance Test

on:
  workflow_dispatch: # Allows manual triggering
  push:
    branches: [main]
  schedule:
    - cron: '0 12 * * *' # Run daily at noon UTC

jobs:
  performance-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4  # Fixed version (was 'va' in error)
    
    - name: Set up Node.js
      uses: actions/setup-node@v3  # Fixed version (was 'va' in error)
      with:
        node-version: '18'
    
    - name: Install Lighthouse CI
      run: npm install -g @lhci/cli
    
    - name: Run Lighthouse test
      run: |
        lhci collect --url="https://www.google.com/search?q=login&rlz=1C1GCEA_enIN1106IN1106&oq=login&gs_lcrp=EgZjaHJvbWUyBggAEEUYOTIHCAEQABiPAjIHCAIQABiPAjIHCAMQABiPAjIGCAQQRRg8MgYIBRBFGDwyBggGEEUYPNIBCDIwNjhqMGoxqAIAsAIA&sourceid=chrome&ie=UTF-8"
        lhci assert --preset=lighthouse:recommended
    
    - name: Upload results
      uses: actions/upload-artifact@v4  # Updated to v4 (was v3 in error)
      with:
        name: lighthouse-results
        path: .lighthouseci/
